framework:
    rate_limiter:
        api_login:
            policy: fixed_window
            limit: 5
            interval: '1 minute'
        api_register:
            policy: fixed_window
            limit: 3
            interval: '1 hour'
        api_social_auth:
            policy: fixed_window
            limit: 10
            interval: '1 minute'
