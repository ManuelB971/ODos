framework:
    default_locale: fr
    translator:
        default_path: '%kernel.project_dir%/translations'
        # Langues exposées à l'app mobile (miroir de SUPPORTED_LANGUAGES côté front).
        enabled_locales: ['fr', 'en', 'ar']
        fallbacks:
            - fr
        providers:
