/**
 * Connexion Google / Apple.
 * Passer `SOCIAL_AUTH_ENABLED` à `true` dans `constants/featureFlags.ts`
 * une fois les client IDs + backend OAuth configurés.
 * @see docs/OAUTH_SETUP.md
 */
export const SOCIAL_AUTH_ENABLED = false;
